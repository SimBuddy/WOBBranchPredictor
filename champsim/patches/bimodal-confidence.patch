diff --git a/branch/bimodal/bimodal.h b/branch/bimodal/bimodal.h
index 151b6dc6..5e791db9 100644
--- a/branch/bimodal/bimodal.h
+++ b/branch/bimodal/bimodal.h
@@ -23,6 +23,8 @@ public:
   // void initialize_branch_predictor();
   bool predict_branch(champsim::address ip);
   void last_branch_result(champsim::address ip, champsim::address branch_target, bool taken, uint8_t branch_type);
+
+  [[nodiscard]] auto confidence(champsim::address ip) const { return bimodal_table[hash(ip)].value(); }
 };
 
 #endif
